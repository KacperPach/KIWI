import kaboom from 'kaboom';
const k = kaboom({});
export const {width,rgb,drawPolygon,deg2rad,height,area, add, pos,mousePos, polygon, circle, vec2, color,opacity,loadShaderURL, uvquad, usePostEffect, rotate,rect, loadShader,anchor, follow, outline, shader} = k
export const {RED, BLACK, GREEN, LEFT, RIGHT, UP, DOWN} = k;
export const {onKeyDown, rad2deg,onUpdate, drawLine, drawCircle, drawRect, onDraw, loadSprite, sprite, onCollide, addKaboom} = k